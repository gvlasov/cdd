#!/usr/bin/env bash
# Start a Codex session for interactive reasoning about the methodology
set -e
cd "$(git rev-parse --show-toplevel)"

exec codex -C "$(pwd)" "$(./processes/refinement/prompt-for-reasoning)"
