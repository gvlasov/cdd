# CDD Manifesto

When a developer clones and opens a project, he must immediately be able to run 

- `up` to bring up the local project's environment
- `tests` to run tests
- `build` to build the project
- `deploy` to deploy the project

and the commands will either work out of the box, or clearly tell what the current development environment is missing.