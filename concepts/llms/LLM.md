CDD is supposed to be convenient for both LLMs and developers. That is achieved by focusing on cohesion, storing related things together and unrelated things apart.

When working with a CDD project, it is very important for an LLM to read `cdd source-code:print` before starting to implement any new requirements, unless explicitly told not to.