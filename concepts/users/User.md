A [[subject]] that has [[beliefs]] about the outcomes of [[Process|processes]] that he can run.

Every systems are built for users. Every system is assumed to have users. A good system knows about its users, specifies and models their intent, [[goals|Goal]], assumptions, preferences.

### Examples
- A user of Google search engine
- A program that calls `grep` (the belief is that it will filter the provided text)