The mechanism by which an application

**Examples:**

- Installation on a user device
- Docker Compose environment
- Local development environment (directly in host operating system)
- Production environment
- CI/CD environment
- For a game, its environment is usually contained in the process of the game
- For a web application, the environment is usually

Examples of environment resources:

- Environment variables
- CPU
- Memory
- GPU
Environment has its own set of environment variables used to configure the program operation.