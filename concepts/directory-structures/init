#!/usr/bin/env bash
# Initialize a CDD directory structure
set -euo pipefail

target="${1:-.}"

mkdir -p \
  "$target/concepts" \
  "$target/processes" \
  "$target/platform/cli" \
  "$target/sandbox"

printf 'Initialized CDD directory structure in %s\n' "$target"
