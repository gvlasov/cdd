services:
  tests:
    build:
      context: ../..
      dockerfile: platform/tests/Dockerfile
    working_dir: /workspace
    volumes:
      - ../..:/workspace
    command:
      - --print-output-on-failure
      - platform/tests/bats
