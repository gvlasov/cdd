; Matches the file store's own limit (FileStore::MAX_BYTES) and Problemos'
; upload_max_filesize, so an upload behaves the same in every environment.
file_uploads=On
upload_max_filesize=10M
post_max_size=12M
