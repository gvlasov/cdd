// An identity is a string that uniquely identifies one instance within an
// ontology. It is the base case of the model: an identity IS its own literal
// content — there is nothing "inside" it to unfold further.
export type Identity = string
